node_modules

# Build outputs
dist/
packages/*/dist/

# Upstream reference clones (read-only, not part of monorepo)
upstream/

# Test coverage
coverage/

# TS incremental build cache
*.tsbuildinfo

# Brainstorming visual-companion mockups (local only)
.superpowers/

# Personal working notes for AI-assisted sessions (local only)
CLAUDE.md

# Local tool output (browser drivers, verification screenshots, packed tarballs)
.playwright-mcp/
.verify-shots/
.local-tarballs/
